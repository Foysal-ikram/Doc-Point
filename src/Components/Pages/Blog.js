import React from 'react';
import UseTitle from '../../Hooks/UseTitle';

const Blog = () => {
    UseTitle('Blog')
    return (
        <div>
            
        </div>
    );
};

export default Blog;