import React from 'react';
import UseTitle from '../../Hooks/UseTitle';

const Contact = () => {
    UseTitle('Contact')
    return (
        <div>
            
        </div>
    );
};

export default Contact;